import { OnClickBgDirective } from './on-click-bg.directive';

describe('OnClickBgDirective', () => {
  it('should create an instance', () => {
    const directive = new OnClickBgDirective();
    expect(directive).toBeTruthy();
  });
});
